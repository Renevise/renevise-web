"use client";

import { SectionBackground } from "@/components/SectionBackground";

/**
 * Hero atmospheric background — thin re-export of the shared
 * SectionBackground so every dark section uses the same visual system.
 */
export function HeroBackground() {
  return <SectionBackground />;
}
